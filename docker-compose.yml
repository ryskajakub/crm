version: '3'
services:
  main:
    build: .
    volumes:
      - "./server:/app/server"
      - "./shared:/app/shared"
      - "./client:/app/client"
    command: ./build-server.sh
    network_mode: "host"
