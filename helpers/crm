server {

  server_name crm;

  root /var/www/crm;

  location /api {
    rewrite ^/api(.*)$ $1 break;
    proxy_pass http://localhost:8000;
  }

  location /client {
    try_files /$uri /client/index.html;
  }

  location / {
    try_files /client/$uri /client/index.html;
  }

}
